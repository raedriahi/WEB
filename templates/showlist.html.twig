<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List of Authors</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f4f4f4;
            padding: 20px;
            margin: 0;
        }
        h1 {
            color: #333;
            text-align: center;
            margin-bottom: 20px;
        }
        .author-container {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            display: flex;
            align-items: center;
            transition: transform 0.2s, box-shadow 0.2s;
            text-decoration: none;
            color: inherit; 
        }
        .author-container:hover {
            transform: scale(1.02);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.5); 
        }
        .author-picture {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            margin-right: 20px;
            object-fit: cover;
            border: 2px solid #4CAF50; 
        }
        .author-details {
            flex-grow: 1;
            line-height: 1.5;
        }
        .author-details h2 {
            margin: 0 0 5px 0;
            text-transform: uppercase;
            color: #4CAF50; 
        }
        .author-details p {
            margin: 5px 0;
            color: #555; 
        }
        .no-authors {
            color: red;
            font-size: 18px;
            text-align: center;
        }
        .author-summary {
            display: flex;
            justify-content: space-between;
            margin-bottom: 20px;
            color: #666;
        }

        a{
            margin-top:10px;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 1px 5px rgba(0, 0, 0, 0.1);
            display:flex;
            transition: transform 0.2s, box-shadow 0.2s;
            text-decoration: none;
            max-width: 120px;
            overflow: hidden;

        }
        h5{
            font-size:1rem;
            text-transform:uppercase;
            color:#4CAF50;
        }
         a:hover {
            transform: translateY(-2px);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.15);
        }
    </style>
</head>
<body>

    <h1>List of Authors</h1>
    <div class="author-summary">
        <p>Nombre total d'auteurs : <strong>{{ authors | length }}</strong></p>
        <p>Nombre total de livres : <strong>{{ totalBooks }}</strong></p>
    </div>

    {% if authors is not empty %}
        {% for author in authors %}
            <div class="author-container">
                <img src="{{ author.picture }}" alt="{{ author.username }}" class="author-picture">
                <div class="author-details">
                    <h2>{{ author.username | upper }}</h2>
                    <p>Email: {{ author.email }}</p>
                    <p>Number of Books: {{ author.nb_books }}</p>
                </div>
                <a href="{{ path('show_author', { id: author.id }) }}" ><h5> Details</h5> </a>
            </div>
        {% endfor %}
    {% else %}
        <p class="no-authors">Aucun auteur disponible pour l'instant.</p>
    {% endif %}

</body>
</html>
